{
  "name": "@netop/server",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "module": "src/index.ts",
  "scripts": {
    "dev": "bun --env-file=../../.env --watch src/index.ts",
    "start": "bun --env-file=../../.env src/index.ts",
    "build": "tsc --noEmit",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@netop/interfaces": "*",
    "@netop/types": "*"
  },
  "devDependencies": {
    "@types/bun": "^1.3.13"
  }
}
